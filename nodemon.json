{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "dist/**"],
  "exec": "ts-node --transpile-only src/index.ts",
  "delay": "250",
  "env": {
    "NODE_ENV": "development"
  }
}
